script:
  python3:
    sudo: true
    commands:
      - 'apt -y install python3'
      - 'apt -y install python3-pip'
  jupyterlab:
    sudo: true
    commands:
      - "pip3 install 'jupyterlab==4.4.10' 'jupyter-server==2.16.0' 'tornado==6.5.8'"
